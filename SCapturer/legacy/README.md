# Legacy prototype

`screen_hotkey_listener.bat` is the original Batch/PowerShell proof of concept.

It is retained only as historical reference. The active implementation is the C# solution rooted at `SCapturer.sln`.
