<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <UseWindowsForms>true</UseWindowsForms>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <AssemblyName>SCapturer.Core</AssemblyName>
    <RootNamespace>SCapturer.Core</RootNamespace>
    <Description>Reference and native capture backends, WIC encoding, display topology, snipping, pipeline, diagnostics, configurable hotkeys, recent captures, settings, and persistence services for SCapturer.</Description>
  </PropertyGroup>
</Project>
